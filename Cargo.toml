[workspace]
members = [
    "rust/scanner",
    "rust/flooder",
    "rust/hasher",
    "rust/dnsx",
    "rust/netprobe",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true